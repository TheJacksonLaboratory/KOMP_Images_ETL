[project]
name = "KOMP_Images_ETL"
version = "0.0.2"
dependencies = [
    "requests",
    "paramiko",
    "mysql.connector",
    'importlib-metadata; python_version>="3.9"',
]
[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "wheel"]